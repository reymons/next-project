{
  "name": "next-project",
  "version": "1.0.0",
  "repository": "https://github.com/reymons/next-project.git",
  "workspaces": [
    "./packages/*"
  ],
  "private": "true",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail 'yarn workspace @np/next-client dev' 'yarn workspace @np/rest-server dev'"
  }
}
